---
interface Props {
  summary: string;
  open?: boolean;
}
const { summary, open = false } = Astro.props;
---

<details class="accordion" open={open}>
  <summary>{summary}</summary>
  <div><slot /></div>
</details>
