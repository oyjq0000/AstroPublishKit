# Keep text files byte-stable across Windows, macOS, and Linux.
* text=auto eol=lf

# Binary assets must never be line-ending normalized.
*.png -text
*.jpg -text
*.jpeg -text
*.gif -text
*.webp -text
*.ico -text
*.woff -text
*.woff2 -text
*.ttf -text
*.otf -text
